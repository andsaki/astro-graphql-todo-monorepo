{
  "name": "astro-graphql-todo-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client",
    "install": "npm install --workspace=client && npm install --workspace=server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "client",
    "server"
  ]
}